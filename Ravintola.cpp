#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;
int ravintola() {

    int people;
    cin >> people;
    vector<int> v;
    for(int i = 1; i <= people; i++) {
        int a, b;
        cin >> a;
        cin >> b;

    }
    return 0;
}